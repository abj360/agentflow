[allowlist]
description = "agentflow gitleaks allowlist"

[[allowlist.rules]]
id = "example-env-values"
description = "Placeholder values in .env.example are not secrets"
path = "(?i)^\.env\.example$"
